// Escreva uma função que usa 2 numeros e retorna o maior entre eles.
function maiorValor(num1, num2) {
    return num1 > num2 ? num1 : num2;
};

let resultado = maiorValor(2, 5);
console.log('maior valor: ', resultado);
