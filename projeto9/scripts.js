// Criar uma função que exibe a quantidade de * que aquela linha possui

function exibirAsteriscos(linhas) {
    let vetor = '';

    for (let linha = 0; linha < linhas; i++) {
        vetor += '*';
        console.log(vetor);
    };
};

exibirAsteriscos(5);
